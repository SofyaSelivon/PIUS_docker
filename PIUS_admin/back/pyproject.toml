[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pius-admin"
version = "0.1.0"

[tool.setuptools]
packages = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
